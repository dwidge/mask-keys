{
  "name": "@dwidge/mask-keys",
  "version": "0.0.1",
  "description": "Mask keys in an object with asterisks",
  "license": "BSL-1.0",
  "author": "DWJ",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "prepare": "rimraf dist && tsc",
    "test": "tsc && node --enable-source-maps --test",
    "dev": "dotenv-flow npm run"
  },
  "dependencies": {},
  "devDependencies": {
    "dotenv-flow-cli": "^1.0.0",
    "expect": "^29.7.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
